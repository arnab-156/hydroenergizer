const Loading = () => {
    return (
        <section className={`w-screen h-screen bg-blue text-white z-96`}>
            <div>
                Loading...
            </div>
        </section>
    )
}

export default Loading;